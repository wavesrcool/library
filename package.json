{
  "author": "Tyson Lupul <js@tysonlupul.com>",
  "bugs": "https://github.com/wavesrcool/library/issues",
  "homepage": "https://github.com/wavesrcool/library#readme",
  "license": "MIT",
  "name": "root",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/wavesrcool/library.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "eslint --ext=.ts packages --fix",
    "lint:all": "lerna run lint --stream --parallel",
    "test": "yarn jest --coverage",
    "rm": "npm exec --workspaces -- npx rimraf node_modules && npx rimraf node_modules"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/jest": "^28.1.8",
    "@types/node": "^18.7.14",
    "husky": "^8.0.1",
    "jest": "^28.0.0",
    "lerna": "^5.1.8",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1"
  },
  "resolutions": {
    "typescript": "^4.8.3"
  }
}
